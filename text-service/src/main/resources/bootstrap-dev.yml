spring:
  cloud:
    nacos:
      config:
        # 使用extension-configs[n]，配置加载多个dataId
        extension-configs:
          - data-id: common-dev.yaml
            group: LLPY
            refresh: true  # 动态刷新配置
          - data-id: common-aliyun.yaml
            group: LLPY
            refresh: true  # 动态刷新配置
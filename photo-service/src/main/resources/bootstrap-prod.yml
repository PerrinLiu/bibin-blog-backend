spring:
  cloud:
    nacos:
      config:
        file-extension: yaml # 配置的后缀名
        # 使用extension-configs[n]，配置加载多个dataId
        extension-configs:
          - data-id: common-prod.yaml
            group: LLPY
            refresh: true  # 动态刷新配置










